using KaifGames.Monobehaviours;
using KaifGames.ScriptableObjects;
using KaifGames.Services;
using UnityEngine;
using VContainer;
using VContainer.Unity;

namespace KaifGames
{
    public class ProgressBarLifeTimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
            
        }
    }
}